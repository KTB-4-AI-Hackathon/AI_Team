[project]
name = "ai-team"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "whale2200d", email = "whale2200d@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
]

[project.scripts]
ai-team = "ai_team:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
